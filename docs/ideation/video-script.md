# Liquid · two-minute video script · draft v2 (04:55 EDT, after the consistency review)

Narration is 232 words. At 150 words per minute that is 93 seconds, at 140 wpm 99 seconds, leaving 21 to 27 seconds of silence for the clock and the title. No beat exceeds its window at 150 wpm. If it still runs long, cut beat 9 first. Record at 1920×1080: iPhone mirrored on the left third, dashboard on the right two thirds. Buyer phones off screen; their messages appear as an inset only in beats 5 to 7.

Rules from the evidence (log iteration 3): the sentence in the first eight seconds; something working by 0:20; every injected fault narrated as injected before the recovery; say "test mode" before a judge does; the word "audit" once; nothing claimed that is not on screen; never present the backup take as live.

Demo clock: 1 simulated hour per real second, with an operator "skip to" control. The envelope hash on screen is `874acade…bb80c`; it is committed at 07:00 before any product code, which is what beat 3 says.

| # | Time | On screen | Narration (spoken) | iMessage content / operator cue |
|---|---|---|---|---|
| 1 | 0:00–0:08 | Title card 2s: **Liquid** · "sell it by your deadline. never below your price." Then the iPhone: seller sends a photo of the iPad and a text. | "Liquid. Text it a photo and a deadline; it sells the thing by then, never below your price." (17) | Seller phone: `[photo]` then `gone by sunday 6pm. not under 220` |
| 2 | 0:08–0:18 | iPhone: the agent's question. Caption: *I12 · value sanity: comps disagree with the model guess by 27% of the comps median* | "First it checks what it's selling. These two iPads look identical; one is worth eighty dollars more. So it asks." (21) | Agent: `is this the ipad air 4th gen or 5th? they look the same and the 5th is worth about $80 more. reply 4 or 5.` Seller: `5` |
| 3 | 0:18–0:30 | iPhone: the frontier card. Dashboard: 48 comps, histogram of 1,000 twin runs, the hash `874acade…bb80c` in the header. Caption: *1,000 simulated markets · predictions committed at 07:00, before the code* | "It runs the item through a thousand simulated markets: what it's worth now, versus by your deadline. Those predictions were committed to git this morning, before the code." (29) | Agent: `your options` / `$221  guaranteed now (bankmycell, today)` / `~$305  likely by sunday · 99% in 1,000 simulated markets` / `64% chance it's gone by tomorrow, at about the same price` / `starting at $355 and coming down as it gets closer. never below $220 without asking you.` |
| 4 | 0:30–0:50 | Dashboard: operator presses "skip to 24h left"; one tick; the price moves $355 → $335; the reason line highlights. Caption: *reprice · 0 inquiries in 48h · arrival estimate 2.0 → 1.1/day · V(24h) = $280* | "Skip ahead two days. No messages, so the engine lowers its demand estimate and the price comes down. Every move shows its reason." (24) | Agent check-in on iPhone: `24h left. plenty of looks, no messages. dropped to $335.` |
| 5 | 0:50–1:05 | Inset: buyer A offers. Dashboard: EV(accept $280) = $272 vs V(24h) = $280 → counter $320. Operator clicks Break Glass "instruction violation"; the Guard panel shows *REJECTED draft "$215 works" · I1 floor · item demo-inject · INJECTED*. | "A buyer offers two-eighty. The engine weighs that against waiting and counters at three-twenty. Now we inject a bad draft: the model writes 'two-fifteen works', below the floor. The Guard blocks it." (33) | Buyer A: `would you do 280?` Agent to A: `can do $320. that's where it sits today.` A: `290 is my max` Agent to A: `noted. if it's still here later i'll come back to you at $290.` |
| 6 | 1:05–1:15 | Inset: buyer B accepts. Dashboard: status PENDING_PAYMENT, Stripe session id, caption *Stripe Checkout · test mode* | "Someone takes it at three-oh-five. Real Stripe, test mode." (10) | Buyer B: `i'll take it at 305` Agent to B: `deal at $305. pay here within 15 minutes: <link>` |
| 7 | 1:15–1:28 | Inset: B pays with `4000 0000 0000 0002`. Dashboard: `payment_intent.payment_failed` → *expire session* → *re-evaluate* → ACCEPT A's standing $290 → new link → `checkout.session.completed`. Caption: *injected fault · declined card · rerouted in 1 tick · one checkout at a time* | "An injected failure: the card declines. The agent expires that checkout and moves to the next best buyer. No double sale, no floor breach, no missed deadline." (27) | Agent to B: `that payment didn't go through. i've offered it to someone else; i'll text you if it falls through.` Agent to A: `it fell through. $290 is yours, pay here within 15 minutes: <link>` A pays with `4242…` |
| 8 | 1:28–1:40 | Dashboard: DoorDash delivery created → picked up (3s, operator advances the simulator). Calendar event 1s. iPhone: the sold text. Caption: *DoorDash Drive sandbox · Google Calendar · ledger row 41 · REAL* | "Paid. A DoorDash courier is dispatched, the pickup is on the calendar, and the seller gets one text: sixty-nine dollars more than the guaranteed offer." (25) | Agent to seller: `sold for $290 to a. that's $69 more than the guaranteed offer. a dasher picks it up today at 4:10, you don't need to be home. it's on your calendar.` |
| 9 | 1:40–1:48 | Dashboard: three counters, agent $306 · static $272 · oracle ⟦from Sunday's grid⟧, labeled *means over 200 seeds* (or the per-seed race if `twin/race.py` is built), all TWIN. | "Luck? Same buyers, three sellers: one simulated buyer stream fed to the agent, a static listing, and an oracle." (19) | Cut this beat first if over time |
| 10 | 1:48–1:56 | Slide: the envelope's four rows beside realized values, the failure matrix with the seven classes and a test file each, the "where we lose" card. Caption: *audit: every decision is a ledger row with its inputs* | "How we know: predictions registered before the build, beside what happened; a test for every failure class in Lemma's taxonomy." (21) | Static slide, 8 seconds |
| 11 | 1:56–2:00 | End card: "real: iMessage · eBay · Stripe · DoorDash · Calendar · Claude. buyers: simulated unless a teammate is typing. every event says which." Repo URL. | "Every event says what was real." (6) | |

## Operator run sheet
- Before the take: `demo/reset.py` (wipe item state, expire open Stripe sessions, cancel open DoorDash deliveries, reset the `demo-inject` item), demo clock paused at t=0, two buyer phones ready with their lines, simulator page open.
- Clock cues: at 0:30 press "skip to 24h left" and let one tick run; pause. Unpause for beats 5 and 6; pause at PENDING_PAYMENT. The decline and reroute happen on webhooks, not the clock.
- Beat 5's rejected draft is the Break Glass "instruction violation" injector fired on the parallel item `demo-inject`; it appears in the Guard panel badged INJECTED and never in the demo item's ledger. If Break Glass is not built, show the leak test's stored example with the caption "from the test suite" and say so.
- Buyer A's $290 stays a standing offer; no checkout is issued to A while B's session is open (I2). The reroute issues A's checkout only after B's is expired.
- Backup take: recorded at 16:00 ET unedited. If used, the end card says "backup take, recorded 16:00, unedited".

## Cut order if the P0.5 twists are not built
Beat 9 → drop. Beat 10 → keep the envelope table and the failure matrix even if "where we lose" is missing. Beat 5 → the injected draft becomes the test-suite example. Beat 3 → keep the histogram if `twin/intake.py` ran, else show the card and say "from our model".
